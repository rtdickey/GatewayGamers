{
    "routes": [{
        "route": "/logout",
        "redirect": "/.auth/logout?post_logout_redirect_uri=http://localhost:4280/"
      }],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}