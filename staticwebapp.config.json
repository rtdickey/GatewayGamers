{
    "trailingSlash": "auto",
    "routes": [{
        "route": "/logout",
        "redirect": "/.auth/logout?post_logout_redirect_uri=/"
    }],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}